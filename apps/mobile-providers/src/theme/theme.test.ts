import { render } from "@testing-library/react-native";
import theme, { fontFamilyStyle } from ".";

describe("Themeing components work", () => {
  it("Something", () => {
    expect(theme).toBeDefined();
  });
});
