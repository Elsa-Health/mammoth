// import { createClient } from "@supabase/supabase-js";

// const { SUPABASE_API_URL, SUPABASE_PUBLIC_ANON_KEY } = process.env;
// if (SUPABASE_API_URL === undefined || SUPABASE_PUBLIC_ANON_KEY === undefined) {
// 	throw new Error("Unable to initialize Supabase.");
// }

// const supabase = createClient(SUPABASE_API_URL, SUPABASE_PUBLIC_ANON_KEY);
